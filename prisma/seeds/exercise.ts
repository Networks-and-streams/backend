import type { SeedContext } from '../seed-context';

/**
 * Placeholder seed for domain data that does not exist yet in this project
 * (left over from a template). No-op until exercise data is modeled.
 */
export async function seedExercise(_ctx: SeedContext): Promise<void> {
  // Intentionally empty.
}
