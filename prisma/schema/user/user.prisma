model User {
  id       String  @id @default(uuid())
  email    String  @unique
  password String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  sessions      Session[] // Active devices
  oauthAccounts OAuthAccount[]
  subscription  Subscription?
  payments      Payment[]
}
